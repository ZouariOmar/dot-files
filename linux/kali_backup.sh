#!/bin/bash

#? Install MySQL-C++ connector
sudo apt-get install libmysqlcppconn-dev

#? install obsidian
sudo wget https://github.com/obsidianmd/obsidian-releases/releases/download/v1.6.3/obsidian_1.6.3_amd64.deb && chmod +x obsidian_1.6.3_amd64.deb && sudo dpkg -i obsidian_1.6.3_amd64.deb

#? install keepassxc
sudo apt install keepassxc

#? install gcc, g++, ...
sudo apt install build-essential

#? install vscode (soft)

#? install balena

#? install drawio (soft)

#? install Postman (soft)

#? install MPLAB X IDE

#? sqllitebrowser (soft)

#? install gnome
sudo apt install gnome

#? install Cmake
sudo apt install cmake

#? Install ccmake
sudo apt install cmake-curses-gui

#? Install gprof
sudo apt-get install binutils

#? install google-chrome
sudo wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb && chmod +x google-chrome-stable_current_amd64.deb && sudo dpkg -i google-chrome-stable_current_amd64.deb

#? install SDL1.2 libs
sudo apt-get install libsdl1.2-dev libsdl-image1.2-dev libsdl-mixer1.2-dev libsdl-ttf2.0-dev libpulse-dev
libxt-dev libsdl-gfx1.2-dev

#? install SDL2.0 libs
sudo apt-get install libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev libpulse-dev libxt-dev libsdl2-gfx-dev

#? Install Arduino
sudo apt install arduino

#? Install Google Test (gtest) in the SE
firefox https://github.com/google/googletest/blob/main/googletest/README.md

#? install npm
sudo apt install npm

#? install valgrind
sudo apt install valgrind

#? install Ninja
sudo apt install ninja-build

#? install Qt5 libs
sudo apt-get install qtbase5-dev qtchooser qt5-qmake qtbase5-dev-tools

#? install Qt6 (app)

#? install stacer

#? install libre-office

#? install OBS

#? install OpenShot (soft)

#? install sqlite3

#? install wine

#? install pascal
sudo apt install poshc2

#? install doxygen
sudo apt install doxygen

#? install Gradle
sudo apt install gradle

#? install libcurl (C/C++)
sudo apt-get install libcurl4-openssl-dev

#? install cJSON (C/C++)
sudo apt-get install libcjson-dev

#? install prettier in the sys env
sudo npm install -g prettier

#? install the php composer
sudo apt install composer

#? install nestjs globally 
sudo npm install -g @nestjs/cli

#? install jupyter-notebook
sudo apt install jupyter-notebook
